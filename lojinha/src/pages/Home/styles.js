import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        marginTop:15,
        alignItems: 'center',
        flexWrap:"wrap",
        flexDirection:"row",
        justifyContent: "space-evenly"

    },
})

